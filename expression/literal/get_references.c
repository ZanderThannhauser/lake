
#include <debug.h>

#include "struct.h"
#include "get_references.h"

int literal_expression_get_references(
	struct expression* super,
	struct strset* references)
{
	int error = 0;
/*	struct literal_expression* const this = (void*) super;*/
	ENTER;
	
	HERE;
	
	EXIT;
	return error;
}

